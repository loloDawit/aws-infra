name: Terraform Deployment

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  terraform:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v2
        with:
          role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/GitHubActionsOIDC
          aws-region: us-west-2

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2

      - name: Terraform Init
        run: |
          terraform init -input=false
        working-directory: envs/dev  # ✅ Run Terraform inside the correct directory

      - name: Terraform Plan
        run: |
          terraform plan -input=false -var-file=terraform.tfvars
        working-directory: envs/dev  # ✅ Run Terraform inside the correct directory

      - name: Terraform Apply
        if: github.ref == 'refs/heads/main'
        run: |
          terraform apply -auto-approve -input=false -var-file=terraform.tfvars
        working-directory: envs/dev  # ✅ Run Terraform inside the correct directory
